# Workspace Rules

1. **Git Commits and Pushes**: Do NOT automatically commit and push changes to GitHub. The user prefers to review and execute git commits and git pushes themselves.
2. **Changelog**: Document all changes, bug fixes, and feature additions in a `CHANGELOG.md` file in the root of the project so the user has a record of everything that has been done.
